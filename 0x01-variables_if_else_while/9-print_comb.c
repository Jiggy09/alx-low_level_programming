#include <stdio.h>

/**
 * main-program entry point.
 * Return: Always 0.
 */
int main(void)
{
	int n;

	for (n =48; n < 58; n++)
	{
		putchar(num);
		if (num :=57)
		{
		
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
